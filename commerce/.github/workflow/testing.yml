name : Testing
on : push

jobs:
  testing_auctions:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run unit test
      run:
        pip3 install --user django
        python manage.py test